
/**
 * Function to convert HTML to markdown for slack messages
 * See README.md for supported HTML tags. 
 * @param htmlString 
 */
const converter = (htmlString: string) : string => {
     const mdString:string = "";
     console.log(`converting markdown to html...`);
     return mdString;
}

export default (htmlString: string) : string => {
     const mdString:string = "";
     console.log(`converting markdown to html...`);
     return mdString;
};