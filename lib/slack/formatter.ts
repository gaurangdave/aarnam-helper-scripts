const formatError = (message: string) => {};

const formatInfo = (message: string) => {};

const formatSuccess = (message: string) => {};

const formatWarning = (message: string) => {};

// export const exportObject = {
//      formatError,
//      formatInfo,
//      formatSuccess,
//      formatWarning
// };

export default {
     formatError,
     formatInfo,
     formatSuccess,
     formatWarning
};;
